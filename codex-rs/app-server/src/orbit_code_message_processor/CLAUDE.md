# app-server/src/orbit_code_message_processor

Contains helper submodules for the `CodexMessageProcessor`, which is the core domain request handler in the app-server. These helpers extract and organize supporting logic that would otherwise bloat the main `orbit_code_message_processor.rs` file.
