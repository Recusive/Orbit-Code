# 004_move_to_new_directory/expected/

Expected filesystem state after applying the 004_move_to_new_directory patch. Contains `old/other.txt` (unchanged) and `renamed/dir/name.txt` (moved and updated).
