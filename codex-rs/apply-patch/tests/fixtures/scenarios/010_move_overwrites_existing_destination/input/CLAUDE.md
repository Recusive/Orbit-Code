# 010_move_overwrites_existing_destination/input/

Initial filesystem state for scenario 010. Contains `old/name.txt` (source), `old/other.txt` (untouched), and `renamed/dir/name.txt` (pre-existing destination to be overwritten).
