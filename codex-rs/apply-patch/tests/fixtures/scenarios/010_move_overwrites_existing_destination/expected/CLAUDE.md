# 010_move_overwrites_existing_destination/expected/

Expected filesystem state after the move-overwrite patch. Contains `old/other.txt` (unchanged) and `renamed/dir/name.txt` (overwritten with "new").
