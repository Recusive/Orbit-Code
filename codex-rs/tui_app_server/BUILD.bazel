load("//:defs.bzl", "orbit_code_rust_crate")

orbit_code_rust_crate(
    name = "tui_app_server",
    crate_name = "orbit_code_tui_app_server",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ) + [
        "//orbit-code-rs/core:templates/collaboration_mode/default.md",
        "//orbit-code-rs/core:templates/collaboration_mode/plan.md",
    ],
    test_data_extra = glob(["src/**/snapshots/**"]) + ["//orbit-code-rs/core:model_availability_nux_fixtures"],
    integration_compile_data_extra = ["src/test_backend.rs"],
    extra_binaries = [
        "//orbit-code-rs/cli:codex",
    ],
)
