//! Placeholder for upstream registry tests.
//!
//! TODO(upstream-0.118.0): restore the copied `registry_tests.rs` coverage once the
//! path-based `AgentRegistry` APIs land on this branch. The current branch still uses
//! `guards.rs` + `control_tests.rs`, so the upstream registry tests would not compile.
