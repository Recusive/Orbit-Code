load("//:defs.bzl", "orbit_code_rust_crate")

orbit_code_rust_crate(
    name = "common",
    crate_name = "core_test_support",
    crate_srcs = glob(["*.rs"]),
    lib_data_extra = [
        "//orbit-code-rs/core:model_availability_nux_fixtures",
    ],
)
