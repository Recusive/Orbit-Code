# app-server-protocol/schema

Root directory for generated schema fixtures that define the app-server protocol contract in language-agnostic formats. These files are auto-generated from Rust types and committed to version control so that cross-language consumers (TypeScript clients, documentation tools, etc.) can consume the...
