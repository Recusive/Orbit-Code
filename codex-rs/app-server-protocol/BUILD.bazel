load("//:defs.bzl", "orbit_code_rust_crate")

orbit_code_rust_crate(
    name = "app-server-protocol",
    crate_name = "orbit_code_app_server_protocol",
    test_data_extra = glob(["schema/**"], allow_empty = True),
)
