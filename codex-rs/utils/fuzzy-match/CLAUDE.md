# codex-rs/utils/fuzzy-match/

Case-insensitive fuzzy subsequence matching with scoring. Returns matched character indices and a score rewarding contiguous and prefix matches. No external dependencies.

## Build & Test
```bash
cargo build -p orbit-code-utils-fuzzy-match
cargo test -p orbit-code-utils-fuzzy-match
```
