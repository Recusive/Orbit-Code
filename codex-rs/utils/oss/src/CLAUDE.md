# codex-rs/utils/oss/src/

Shared utilities for local/OSS model providers (LM Studio and Ollama). Provides default model lookup and async readiness checks for each provider.

## Build & Test
```bash
cargo build -p orbit-code-utils-oss
cargo test -p orbit-code-utils-oss
```
