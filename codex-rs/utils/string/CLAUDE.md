# codex-rs/utils/string/

String utilities: byte-boundary-safe truncation (prefix and suffix), UUID extraction via regex, metric tag value sanitization, and markdown location suffix normalization.

## Build & Test
```bash
cargo build -p orbit-code-utils-string
cargo test -p orbit-code-utils-string
```
